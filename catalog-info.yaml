apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: document-scanner-ios
  description: |
    Scan documents with iOS devices using the VisionKit framework.
  tags:
    - ui
  annotations:
    github.com/project-slug: sda-se/document-scanner-ios
    sda.se/summary:
      $text: ./README.md
spec:
  type: library
  owner: team-communications
  lifecycle: experimental
  system: submission